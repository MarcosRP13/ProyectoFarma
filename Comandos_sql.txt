SELECT Farmacia.nom_farm, clientes.nom_clie from Farmacia inner join pedidos on farmacia.cod_farm = pedidos.cod_farm inner join clientes on pedidos.cod_clie = clientes.cod_clie where farmacia.cod_farm in (select cod_farm from producto where cod_prod = 101);
